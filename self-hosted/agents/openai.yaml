interface:
  display_name: "Remote Login Relay"
  short_description: "Hand a blocked Chrome login page to your phone"
  default_prompt: "When browser work reaches a human-only login step, use $remote-login-relay; guide me through email and tunnel setup if needed, then send a ToolArks link for that exact Chrome page to my phone."
